extends layout.pug

block head
    title Home

block content
    if user
        h1
            | Hello
            span= ` ${user.name}`
        a(href="/users/logout") Logout
    else
        h1 Hello world